
#include <stdio.h>

int main() {
  int l,w,area;
    printf("enter the value of l\n");
    scanf("%d",&l);
    printf("enter the value of w\n");
    scanf("%d",&w);
    area=l*w;
    printf("area=%d\n",area);
    return 0;
}
